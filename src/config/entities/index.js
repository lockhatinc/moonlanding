export const allSpecs = {};

console.warn(
  'DEPRECATED: Entity config files have been deleted. ' +
  'All entity specifications should now be generated from master-config.yml. ' +
  'The allSpecs export is currently empty and needs to be populated by parsing master-config.yml'
);
